# Prompt Log — Workshop สัปดาห์ที่ 11 (สร้างแอป Streamlit · ทีมรีเทค 006-022)

สรุป prompt ที่ทีมใช้สั่ง Claude ตามขั้นตอนในเวิร์กชอป (steps 1–9) — ใช้สูตร prompt 5 ส่วน
(บริบท · ข้อเท็จจริง · เงื่อนไข · รูปแบบผลลัพธ์ · การตรวจผล)

---

## ขั้น 1 · ตั้งเครื่อง

**prompt (ย่อ):**
> เครื่องเป็น Windows 11 ยังไม่เคยรัน Python ในเครื่อง (ใช้แต่ Google Colab)
> ช่วยบอกวิธีติดตั้ง Python 3.10+ ให้ครบ (ต้องติ๊ก Add python.exe to PATH) วิธีสร้าง virtual environment
> และวิธี activate ใน PowerShell ถ้าโดนบล็อกสคริปต์ให้แก้อย่างไร

**Claude ตอบ:** ขั้นตอนติดตั้ง + `python -m venv venv` + `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`

**ทีมตรวจ:** ✅ สร้าง `venv` และติดตั้ง `streamlit`, `scikit-learn`, `pandas`, `joblib` สำเร็จ

---

## ขั้น 2 · ตรวจไฟล์โมเดล

**prompt (ย่อ):**
> ฉันมีไฟล์ `telco_churn.joblib` ที่เทรนด้วย `DecisionTreeClassifier(max_depth=3, random_state=42)`
> จากข้อมูล Telco Customer Churn (หลัง one-hot แล้ว 12 คอลัมน์) ข้อมูล**ไม่ได้สเกล**
> ช่วยเขียนโค้ดสั้น ๆ เพื่อตรวจว่าโมเดลต้องการคอลัมน์อะไรบ้าง เรียงตามลำดับไหน และต้องสเกล input หรือไม่

**Claude ตอบ:** ให้โค้ดอ่าน `model.feature_names_in_`, `model.classes_`, `model.get_params()`

**ทีมตรวจ:** ✅ ได้รายชื่อ 12 ฟีเจอร์ตามลำดับจริง และยืนยันว่าไม่ต้องสเกล → บันทึกไว้ใน `model_info.json`

---

## ขั้น 3 · สร้าง app.py

**prompt (ย่อ):**
> ช่วยเขียน `app.py` ด้วย Streamlit ให้:
> - โหลด `telco_churn.joblib` ด้วย `joblib.load` ผ่าน `@st.cache_resource`
> - สร้างฟอร์มรับค่าตามฟีเจอร์ 12 ตัว โดย**สร้างจาก `model.feature_names_in_` อัตโนมัติ**
>   (คอลัมน์ตัวเลขใช้ `number_input`, คอลัมน์ 0/1 ใช้ `checkbox`)
> - ทำนายด้วย `predict()` และ `predict_proba()` แล้วแสดงผลเป็น % ความน่าจะเป็นยกเลิกบริการ
> - แปลผลเป็นคำแนะนำเชิงธุรกิจ (เสี่ยง → ติดต่อเชิงรุก · ไม่เสี่ยง → ดูแลตามปกติ)
> ขอโค้ดทั้งไฟล์ + อธิบายว่าบรรทัดไหนทำอะไร

**Claude ตอบ:** โค้ดครบไฟล์ ใช้ `st.form`, `st.columns`, `st.metric`, `st.progress`, `st.expander`

**ทีมตรวจ:** ✅ รันได้ ไม่เกิด KeyError เพราะฟอร์มสร้างจากรายชื่อฟีเจอร์ของโมเดลโดยตรง

---

## ขั้น 4–5 · รัน ทดสอบ และแก้ error

**prompt (ย่อ):**
> รันแล้วเจอ error นี้: [วาง traceback ทั้งก้อน]
> ช่วยอธิบายว่าเกิดจากบรรทัดไหน แก้ให้ และบอกวิธีเช็กเองว่าเกิดจากชื่อ/ลำดับคอลัมน์หรือไม่

**Claude ตอบ:** ชี้จุดที่ชื่อคอลัมน์/ชนิดข้อมูลไม่ตรง และแนะนำให้เทียบ `payload.columns.tolist()` กับ `model.feature_names_in_`

**ทีมตรวจ:** ✅ แก้แล้วรันผ่าน · เพิ่มการบังคับ `columns=MODEL_FEATURES` ใน `build_input()` ทุกครั้ง

---

## ขั้น 6–7 · ทำความเข้าใจโค้ด + ปรับ UI

**prompt (ย่อ):**
> อธิบายโค้ดส่วนเหล่านี้ให้ทีมฟังแบบเข้าใจง่าย: `@st.cache_resource` ทำอะไร,
> `predict()` ต่างจาก `predict_proba()` อย่างไร,
> และช่วยปรับ UI ให้คนไม่รู้บริบทใช้เองได้ (ใส่ `help=` ทุกช่อง, จัด 2 คอลัมน์,
> เพิ่ม `st.expander` ให้ตรวจค่าที่ส่งเข้าโมเดล) โดยห้ามเปลี่ยนตรรกะการทำนาย

**Claude ตอบ:** อธิบายทีละส่วน + ปรับ UI ตามที่ขอ

**ทีมตรวจ:** ✅ สมาชิกอธิบายโค้ดได้ครบ 4 ส่วน (cache / build_input / predict vs proba / การแปลผลเป็นคำแนะนำ)

---

## ขั้น 8 · ใช้โมเดลและฟีเจอร์จริงของทีม

**prompt (ย่อ):**
> โมเดลนี้เป็นของทีมอยู่แล้ว แต่ทีมต้องการให้แอปยืนยันกับผล GA สัปดาห์ที่ 10
> ซึ่งคัดเหลือ 2 ฟีเจอร์ (`tenure`, `InternetService_Fiber optic`) ได้ความแม่นเท่าเดิม (CV 0.7901)
> ช่วยบอกว่าถ้าจะทำเวอร์ชันฟอร์มสั้น ต้องเทรนโมเดลใหม่บน 2 ฟีเจอร์นั้นหรือไม่ และมีข้อควรระวังอะไร

**Claude ตอบ:** ต้องเทรนใหม่ (เพราะโมเดลเดิมคาดหวัง 12 คอลัมน์) และเตือนให้ตรวจ `feature_names_in_` ของโมเดลใหม่

**ทีมตรวจ:** ✅ เทรนใหม่บน 2 ฟีเจอร์ได้ test 0.7871 / CV 0.7901 เท่าเดิมจริง → เก็บเป็นเวอร์ชันทางเลือก

---

## ขั้น 9 · เตรียมไฟล์ deploy

**prompt (ย่อ):**
> ฉันมีแอปที่รันในเครื่องได้แล้ว จะ deploy ขึ้น Streamlit Community Cloud สัปดาห์หน้า
> โครงสร้าง: `app.py`, `telco_churn.joblib`, `model_info.json`
> ไลบรารีพร้อมเวอร์ชันจาก pip freeze: streamlit 1.64.0 · scikit-learn 1.9.1 · pandas 3.0.6 · numpy 2.5.3 · joblib 1.6.0
> ช่วยเขียน `requirements.txt` ที่ล็อกเวอร์ชัน, `README.md` ภาษาไทย, `.gitignore`
> และเช็กลิสต์ก่อน deploy (พาธสัมพัทธ์ · ข้อมูลส่วนบุคคล · repo สาธารณะ/ส่วนตัว)

**Claude ตอบ:** ให้ไฟล์ทั้งสาม + เช็กลิสต์

**ทีมตรวจ:** ✅ พาธโมเดลใช้ `Path(__file__).resolve().parent` (สัมพัทธ์) · โมเดลเล็ก ~3 KB · ไม่มีข้อมูลส่วนบุคคล

---

## จุดที่ Claude ตอบผิด / ไม่ตรงโจทย์ และทีมจับได้เอง

| ครั้ง | สิ่งที่ Claude ให้มา | ปัญหา | ทีมแก้อย่างไร |
|---|---|---|---|
| 1 | สร้าง DataFrame แบบ hardcode 12 คอลัมน์ | ถ้าเปลี่ยนโมเดล ฟอร์มจะไม่ตรงและ error | เปลี่ยนเป็นสร้างจาก `model.feature_names_in_` อัตโนมัติ |
| 2 | แนะนำให้สเกล input ด้วย MinMax "เพื่อความปลอดภัย" | ไม่ตรงกับข้อมูลทีมที่ไม่ได้สเกล → ผลจะผิด | ยึดข้อเท็จจริงของทีม (ไม่สเกล) และทดสอบกับค่าจริง |
| 3 | ใช้ `predict()` อย่างเดียว | ไม่แสดงความน่าจะเป็นซึ่งแอปต้องใช้ตัดสินที่เกณฑ์ 0.5 | ใช้ `predict_proba()` แล้วหยิบ P(1) มาแสดงเป็น % |
